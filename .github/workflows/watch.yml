name: ad-api-notice-watch

on:
  schedule:
    - cron: '23 0,3,6,9 * * *' # 매일 09:23, 12:23, 15:23, 18:23 KST (UTC 00/03/06/09시) — 정각은 혼잡으로 지연/누락이 잦아 23분으로 분산
  workflow_dispatch:

permissions:
  contents: write
  issues: write # 수집 이상 소스를 parser-health 이슈로 발행
  id-token: write # claude-code-action OIDC 토큰 교환용

jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Crawl sources
        run: python scripts/crawl.py

      - name: Check for new items
        id: check
        run: |
          if [ -f out/new_items.json ]; then
            echo "has_new=true" >> "$GITHUB_OUTPUT"
            echo "신규 공지 있음"
          else
            echo "has_new=false" >> "$GITHUB_OUTPUT"
            echo "신규 공지 없음"
          fi

      - name: Summarize with Claude
        if: steps.check.outputs.has_new == 'true'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }} # Claude GitHub App 설치 없이 기본 토큰 사용
          prompt: "out/new_items.json을 .claude/skills/notice-summary 기준으로 요약해 out/summary.md와 out/summaries.json으로 저장해줘"
          claude_args: "--allowedTools Read,Write"

      - name: Archive notices
        if: steps.check.outputs.has_new == 'true'
        run: python scripts/archive.py # 슬랙 발송 전 — 웹훅이 실패해도 기록은 남긴다

      - name: Send Slack notification
        if: steps.check.outputs.has_new == 'true'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: python scripts/notify.py

      - name: Commit seen.json & archive
        run: |
          git add state/seen.json
          if [ -d archive ]; then git add archive; fi
          if ! git diff --cached --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git commit -m "chore: seen/archive update $(date -u +%Y-%m-%d)"
            git push
          else
            echo "변경 없음"
          fi

      - name: Report parser health
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/report_health.py
